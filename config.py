# modbusx/config.py

"""
Configuration loading and saving (stub).
To expand: YAML/JSON config reading and CLI parsing.
"""

def load_config(path):
    # Load config (future)
    return {}

def save_config(path, config):
    # Save config (future)
    pass